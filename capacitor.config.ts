import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.herocoin.app',
  appName: 'herocoin',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
